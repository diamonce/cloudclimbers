apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wordpress-v20-0-1
  namespace: wordpress-v20-0-1
spec:
  chart:
    spec:
      version: ">=1.0.0"
  values:
    ingress:
      hosts:
        - host: wordpress-v20-0-1.cloudclimbers.xyz
          paths:
            - path: /
              pathType: Prefix
        - host: wordpress-v22-3-1.cloudclimbers.xyz
          paths:
            - path: /
              pathType: Prefix
