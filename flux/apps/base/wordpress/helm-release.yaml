apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: wordpress
  namespace: wordpress
spec:
  releaseName: wordpress
  chart:
    spec:
      chart: wordpress
      version: 22.3.1
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: wordpress
  interval: 5m0s
