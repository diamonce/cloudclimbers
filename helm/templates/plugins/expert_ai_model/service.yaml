apiVersion: v1
kind: Service
metadata:
  name: expert-ai-model
  labels:
    app: expert-ai-model
spec:
  ports:
    - port: 8084
      targetPort: 8084
  selector:
    app: expert-ai-model
  type: ClusterIP
