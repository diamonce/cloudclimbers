apiVersion: apps/v1
kind: Deployment
metadata:
  name: delete-plugin
  labels:
    app: delete-plugin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: delete-plugin
  template:
    metadata:
      labels:
        app: delete-plugin
    spec:
      containers:
        - name: delete-plugin
          image: {{ .Values.plugins.delete.image }}
          ports:
            - containerPort: 8083
          volumeMounts:
            - name: gcp-service-account
              mountPath: /var/secrets/google
              readOnly: true
          env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /var/secrets/google/service-account-key.json
            - name: ENCRYPTION_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.gcpServiceAccount.secretName }}
                  key: encryptionPassword
      initContainers:
        - name: decrypt-service-account-key
          image: alpine
          command: ["sh", "-c", "apk add openssl && openssl enc -d -aes-256-cbc -in /var/secrets/google/service-account-key.json.enc -out /var/secrets/google/service-account-key.json -k $ENCRYPTION_PASSWORD"]
          volumeMounts:
            - name: gcp-service-account
              mountPath: /var/secrets/google
              readOnly: false
      volumes:
        - name: gcp-service-account
          secret:
            secretName: {{ .Values.gcpServiceAccount.secretName }}
