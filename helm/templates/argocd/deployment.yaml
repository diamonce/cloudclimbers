apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd
  namespace: argocd
  labels:
    app: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: argocd
  template:
    metadata:
      labels:
        app: argocd
    spec:
      containers:
        - name: argocd
          image: argoproj/argocd:latest
          ports:
            - containerPort: 8080
